{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "enabled": false,
  "enabledManagers": ["npm"],
  "packageRules": [
    {
      "description": "Upstream unlayer packages — auto-create PRs on every new version",
      "matchPackageNames": [
        "@unlayer/exporters",
        "@unlayer/types"
      ],
      "enabled": true,
      "groupName": "unlayer upstream",
      "schedule": ["at any time"],
      "automerge": false,
      "rangeStrategy": "replace"
    }
  ]
}
